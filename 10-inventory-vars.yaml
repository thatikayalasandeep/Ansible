- name: Play to access variables from inventory for localhost
  hosts:  web

  task:
    - name: Print course information
      ansible.builtin.debug:
        msg: "Course is {{ COURSE }}, Duration is {{ DURATION }}, Trainer is {{ TRAINER }}"
