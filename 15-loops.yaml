- name: Learn loops concept
  hosts:  local
  connection:  local

  taks:
  - name: Print items using loop
    ansible.builtin.debug:
      msg: "{{ item }}"
    loop:
    - sandeep
    - tharak
    - prince
    - sitara